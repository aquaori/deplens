const { analyzeProject } = require('./dist'); const { createReviewRuntime } = require('./dist/agent/base'); (async () = const report = await analyzeProject({ path: process.cwd(), verbose: false, silence: true, ignoreDep: '', ignorePath: '', ignoreFile: '', config: '', json: false, output: '', html: false }, false); const runtime = createReviewRuntime(report); const answer = await runtime.ask('这个项目有哪些未使用依赖？'); console.log(JSON.stringify(answer, null, 2)); })().catch(err = console.error(err); process.exit(1); });  
